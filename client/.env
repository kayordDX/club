PUBLIC_API_URL=http://localhost:5000
PUBLIC_APP_URL=http://localhost:5173
PUBLIC_IDENTITY_URL=http://localhost:8088/realms/kayord

# Session cookie encryption key (server-only, >= 32 chars). Optional in dev —
# an ephemeral per-process key is used if unset. REQUIRED in production, where it
# must be set in the runtime environment (not a committed .env file).
# SESSION_SECRET=
